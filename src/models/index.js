import Example from "./Example";
import Block from "./Block";
import Account from "./Account";

const models = {
  example: new Example(),
  block: new Block(),
  account: new Account()
};

export default models;
